<Page
    x:Class="FlipNLearn.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlipNLearn"
    xmlns:converter="using:FlipNLearn.ValueConverters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converter:ColorToBrushConverter x:Key="ColorToBrushConverter" />
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="7*" />
        </Grid.ColumnDefinitions>
        
        <!-- Sets Column -->
        <Grid Grid.Column="0" Background="#0C6398">
            <ListView x:Name="ListViewSets" ItemsSource="{Binding Sets}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel HorizontalAlignment="Stretch"
                                    Background="{Binding Color, Converter={StaticResource ColorToBrushConverter}}"
                                    Tapped="Set_Tapped">
                            <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" FontSize="17"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,3,0,5">
                                <TextBlock Text="{Binding Decks.Count}" HorizontalAlignment="Center" FontSize="11" />
                                <TextBlock Text="Decks" Margin="5,0,0,0" FontFamily="Global User Interface" FontSize="11"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        
        <!-- Deck Column -->
        <Grid Grid.Column="1" Background="#006BB2">
            <TextBox Text="{Binding NameBox, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin="30,500,32,0" />
            <ListView x:Name="ListViewDecks" ItemsSource="{Binding SelectedSet.Decks}" Margin="0,0,0,90">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel HorizontalAlignment="Stretch"
                                    Background="{Binding Color, Converter={StaticResource ColorToBrushConverter}}"
                                    Tapped="Set_Tapped">
                            <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" FontSize="17"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,3,0,5">
                                <TextBlock Text="{Binding Cards.Count}" HorizontalAlignment="Center" FontSize="11" />
                                <TextBlock Text="Card(s)" Margin="5,0,0,0" FontFamily="Global User Interface" FontSize="11"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="AddSetButton" Click="AddSetButton_Click" Icon="DockLeft" />
            <AppBarButton x:Name="AddDeckButton" Click="AddDeckButton_Click" Icon="Add"/>
        </CommandBar>
    </Page.BottomAppBar>
</Page>